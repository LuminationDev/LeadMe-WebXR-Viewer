declare class LeadMeLabsConnection {
    private logCallback;
    private pauseCallback;
    private resumeCallback;
    private shutdownCallback;
    private detailsCallback;
    private actionCallback;
    private server;
    private client;
    connect(): void;
    disconnect(): void;
    send(message: string): void;
    setLogCallback(callback: () => void): void;
    setPauseCallback(callback: () => void): void;
    setResumeCallback(callback: () => void): void;
    setShutdownCallback(callback: () => void): void;
    setDetailsCallback(callback: () => void): void;
    setActionCallback(callback: (message: string) => void): void;
    private handleMessage;
}

export { LeadMeLabsConnection };
